{
  "scripts": {
    "build": "bundle exec jekyll build --quiet",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "qa": "npm run format:check && npm run build",
    "qa:portfolio": "node bin/portfolio-qa.cjs"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "1.11.0",
    "playwright": "^1.61.1",
    "prettier": "3.9.5"
  }
}
