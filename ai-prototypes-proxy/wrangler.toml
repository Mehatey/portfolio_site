name = "ai-prototypes-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"
workers_dev = true

# After `npx wrangler login`, deploy then attach these routes in the dashboard
# (Workers & Pages → ai-prototypes-proxy → Settings → Domains & Routes → Add route)
# OR uncomment and fill your zone id if you prefer wrangler.toml routes:
#
# [[routes]]
# pattern = "siddharthmehta.design/inat-audio/*"
# zone_name = "siddharthmehta.design"
#
# [[routes]]
# pattern = "www.siddharthmehta.design/inat-audio/*"
# zone_name = "siddharthmehta.design"
