{% comment %}
  Unified footer. Render the same custom site footer used across the portfolio so
  default-template pages (blog, news, books, CV) match the rest of the site instead
  of showing the legacy stock footer. The custom footer adapts to al-folio's
  light/dark themes via its [data-theme="light"] overrides.
{% endcomment %}
{% if site.newsletter.enabled %}
  {% include newsletter.liquid %}
{% endif %}
{% include site_footer.html %}
