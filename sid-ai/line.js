const stage=document.querySelector('#stage'),motion=document.querySelector('#motionPath'),pencil=document.querySelector('#pencil'),face=document.querySelector('#faceHit'),eyes=[...document.querySelectorAll('.eye')],pupils=[document.querySelector('#pupilL'),document.querySelector('#pupilR')],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
let mx=.5,my=.5,tx=.5,ty=.5,blinkTimer=0,animationStart=performance.now(),duration=3480;
function restart(){stage.classList.remove('ready');void stage.offsetWidth;animationStart=performance.now();stage.classList.add('ready')}
function blink(){if(blinkTimer)return;eyes.forEach(e=>{e.style.animation='none';e.style.transform='scaleY(.06)'});blinkTimer=setTimeout(()=>{eyes.forEach(e=>{e.style.transform='scaleY(1)'});blinkTimer=setTimeout(()=>{eyes.forEach(e=>{e.style.animation=''});blinkTimer=0},240)},115)}
addEventListener('pointermove',e=>{tx=e.clientX/innerWidth;ty=e.clientY/innerHeight});face.addEventListener('pointerenter',()=>stage.classList.add('engaged'));face.addEventListener('pointerleave',()=>stage.classList.remove('engaged'));face.addEventListener('click',()=>{blink();stage.classList.remove('pulse');void stage.offsetWidth;stage.classList.add('pulse','happy');setTimeout(blink,620);setTimeout(()=>stage.classList.remove('pulse','happy'),900)});face.addEventListener('dblclick',restart);addEventListener('keydown',e=>{if(e.key.toLowerCase()==='r')restart()});
function frame(now){mx+=(tx-mx)*.055;my+=(ty-my)*.055;if(!reduced){stage.style.transform=`perspective(1000px) rotateX(${(my-.5)*-3.2}deg) rotateY(${(mx-.5)*4.4}deg)`;const elapsed=Math.min(1,(now-animationStart-200)/duration),length=motion.getTotalLength(),point=motion.getPointAtLength(Math.max(0,elapsed)*length),ahead=motion.getPointAtLength(Math.min(length,Math.max(0,elapsed)*length+2)),angle=Math.atan2(ahead.y-point.y,ahead.x-point.x)*180/Math.PI;pencil.setAttribute('transform',`translate(${point.x} ${point.y}) rotate(${angle})`)}const gazeX=(mx-.5)*11,gazeY=(.5-my)*8;pupils[0].setAttribute('transform',`translate(${gazeX} ${gazeY})`);pupils[1].setAttribute('transform',`translate(${gazeX} ${gazeY})`);requestAnimationFrame(frame)}
stage.classList.add('ready');requestAnimationFrame(frame);setInterval(()=>{if(document.visibilityState==='visible'&&performance.now()-animationStart>4500)blink()},5600);
